# esbuild ships a platform binary and its install script links it. Declared
# here rather than approved interactively so a fresh clone builds without a
# prompt.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
