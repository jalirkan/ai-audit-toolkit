"""Shared domain model: evidence records, statistics, canonical encoding.

``core`` depends on nothing else in the toolkit. Everything else depends on it.
Keeping the arrow pointing one way is what stops the journal from needing to
import probes in order to store their output.
"""
