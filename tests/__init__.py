"""Unit tests.

Run from the repository root:

    python -m unittest discover -s tests -t .

The whole suite runs offline and requires no API key. That is a hard
constraint, not a convenience -- see DECISIONS D-001.
"""
